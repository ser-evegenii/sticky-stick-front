"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
var react_is_1 = require("react-is");
function getFixtureNames(fixtures) {
    return Object.keys(fixtures).reduce(function (prev, fixturePath) {
        var _a;
        var fixtureExport = fixtures[fixturePath];
        return __assign({}, prev, (_a = {}, _a[fixturePath] = isNodeMap(fixtureExport)
            ? Object.keys(fixtureExport)
            : null, _a));
    }, {});
}
exports.getFixtureNames = getFixtureNames;
function getFixtureNode(fixtureExport, fixtureName) {
    if (fixtureName === null) {
        if (isNodeMap(fixtureExport)) {
            throw new Error("Fixture name missing in multi fixture");
        }
        return fixtureExport;
    }
    if (!isNodeMap(fixtureExport)) {
        throw new Error("Fixture name not found in single fixture: " + fixtureName);
    }
    return fixtureExport[fixtureName];
}
exports.getFixtureNode = getFixtureNode;
function isNodeMap(fixtureExport) {
    return (fixtureExport !== null &&
        typeof fixtureExport === 'object' &&
        !react_is_1.isElement(fixtureExport));
}
